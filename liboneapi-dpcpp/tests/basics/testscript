: icx compile test
{
  $path($icx) -c $path($hello) -o hello.o &hello.o >! 2>! == 0
}

: icpx compile test
{
  $path($icpx) -c $path($hello) -o hello.o &hello.o >! 2>! == 0
}

: icpx openmp header test
{
  $path($icpx) -fiopenmp -c $path($openmp) -o openmp.o &openmp.o >! 2>! == 0
}
