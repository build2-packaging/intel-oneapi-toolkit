intf_libs = # Interface dependencies.
impl_libs = # Implementation dependencies.
#import xxxx_libs += libhello%lib{hello}

lib{oneapi-dpcpp}: {hxx ixx txx cxx}{**} $impl_libs $intf_libs

# Build options.
#
cxx.poptions =+ "-I$out_root" "-I$src_root"

#obja{*}: cxx.poptions += -DDPCPP_STATIC_BUILD
#objs{*}: cxx.poptions += -DDPCPP_SHARED_BUILD

# Export options.
#
lib{oneapi-dpcpp}:
{
  cxx.export.poptions = "-I$out_root" "-I$src_root"
  cxx.export.libs = $intf_libs
}

#liba{oneapi-dpcpp}: cxx.export.poptions += -DDPCPP_STATIC
#libs{oneapi-dpcpp}: cxx.export.poptions += -DDPCPP_SHARED

# For pre-releases use the complete version to make sure they cannot
# be used in place of another pre-release or the final version. See
# the version module for details on the version.* variable values.
#
if $version.pre_release
  lib{oneapi-dpcpp}: bin.lib.version = "-$version.project_id"
else
  lib{oneapi-dpcpp}: bin.lib.version = "-$version.major.$version.minor"

# Install into the dpcpp/ subdirectory of, say, /usr/include/
# recreating subdirectories.
#
{hxx ixx txx}{*}:
{
  install         = include/dpcpp/
  install.subdirs = true
}
